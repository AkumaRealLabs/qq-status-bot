services:
  app:
    image: ${APP_IMAGE:-ghcr.io/akumareallabs/ai-upstream-monitor:latest}
    build: .
    ports:
      - "8090:8090"
    environment:
      BROWSER_DEBUG_URL: http://browser:9222
      BROWSER_VNC_URL: http://127.0.0.1:6080/vnc.html?autoconnect=true&resize=scale
      TZ: Asia/Shanghai
    volumes:
      - ./pb_data:/app/pb_data
    depends_on:
      - browser
    restart: unless-stopped

  browser:
    image: ${BROWSER_IMAGE:-ghcr.io/akumareallabs/ai-upstream-monitor-browser:latest}
    build:
      context: .
      dockerfile: browser/Dockerfile
    shm_size: 1gb
    ports:
      - "127.0.0.1:6080:6080"
    volumes:
      - browser_profile:/profile
    restart: unless-stopped

volumes:
  browser_profile:
