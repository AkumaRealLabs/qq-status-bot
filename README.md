# AI Upstream Monitor

Go + SQLite/PostgreSQL + React/Vite admin for monitoring `new-api` and `sub2api` upstream balances and `gpt-5.5` probe status.

```bash
docker compose up -d --build
```

The app listens on `127.0.0.1:8090`. The first visit creates the administrator account.
